# Changelog v1.5.6

## [1.5.6] - 2026-03-07

### Added
- Comprehensive database schema documentation in `project_analysis_summary.md`.
- Detailed JSONB structure mapping for `projects.project_details`.
- Comprehensive Zod Schemas for `vendors` (`vendorSchema.js`) and `domains` (`domainSchema.js`), loaded with `.describe()` contextual usage attributes to ensure strict JSONB validation rules across application layers.
- Added retroactive `.describe()` tags to existing `projectSchema.js`.

### Changed
- Moved research scripts (`check_all_schemas.js`, `inspect_jsonb.js`, etc.) to the `_cleanup` folder to maintain project root cleanliness.
- Updated `MEMORY.md` with session details for Phase 29 and Phase 30.
- Refactored `/admin/vendor-manager/actions.js` and `/admin/domains-manager/actions.js` to replace manual object construction with Zod `safeParse` processing, solidifying UI error catching.
