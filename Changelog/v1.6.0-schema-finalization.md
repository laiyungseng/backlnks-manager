# v1.6.0 — Phase 24 Schema Migration Finalization

**Date:** 2026-03-04

## Summary
Completed the Phase 24 database schema consolidation. All references to the deprecated `project_list` and `project_targets` tables have been fully removed from the codebase. Legacy tables are now dropped from production.

## Changes

### `src/app/admin/actions.js`
- **Removed** invalid legacy sweep calls to `project_targets` and `project_list` (tables are dropped; calls were causing DB errors).
- Delete flow is now clean: `placements` → `projects_hub` → `projects`.

### `src/app/admin/placements/page.jsx`
- **Updated** stale comment that still referenced `project_list` and `project_targets`. Now reflects actual `projects_hub` schema.

## Status
- All admin dashboard pages (`/admin`, `/admin/placements`, SSE route) use `projects_hub` exclusively.
- Delete button on Dashboard now works correctly end-to-end.
